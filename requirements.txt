# ── Phase 1: Core Dependencies ───────────────────────────────────────────
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=3.0.0
langchain-groq>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
typing_extensions>=4.12.0
python-dotenv>=1.0.0

# Search
tavily-python>=0.5.0

# Observability
langfuse>=3.0.0

# Backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Frontend
streamlit>=1.40.0
requests>=2.32.0

# ── Phase 2: Active Dependencies ─────────────────────────────────────────
httpx>=0.27.0               # Scraper Agent — HTTP client
beautifulsoup4>=4.12.0      # Scraper Agent — HTML parsing
lxml>=5.2.0                 # Scraper Agent — faster HTML parser (BS4 backend)
pytest>=8.3.0               # Testing
pytest-cov>=6.0.0           # Coverage reports

# Playwright is optional — JS-heavy page rendering.
# Uncomment and run: playwright install chromium
# playwright>=1.44.0

# ── Phase 3+: Planned Dependencies (uncomment when needed) ───────────────
# matplotlib>=3.9.0           # Visual Agent
# plotly>=5.24.0              # Visual Agent
# sentence-transformers>=3.3  # Embedding for RAG
# qdrant-client>=1.12.0       # Vector store
# ruff>=0.8.0                 # Linting
